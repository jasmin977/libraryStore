{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}
{%  block sliders %} {% endblock %}
{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

  


    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}" class="btn btn-warning">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
        <a href="{{ path('categorie_index') }}" class="btn btn-secondary btn-lg btn-block">back to list</a>

{% endblock %}
